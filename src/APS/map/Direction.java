package APS.map;

// (Encapsulamento) - Uso de estrutura enum para limitar, agrupar e proteger as direções válidas de movimento no mapa
public enum Direction {
    NORTE,
    SUL,
    LESTE,
    OESTE
}